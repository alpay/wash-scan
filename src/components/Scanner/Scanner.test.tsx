import React from 'react';
import { shallow } from 'enzyme';

import Scanner from './Scanner';

// it('renders correctly Scanner', () => {
//   const wrapper = shallow(<Button primary>Primary Button</Button>);

//   expect(wrapper).toMatchSnapshot();
// });
