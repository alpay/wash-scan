import React from 'react';
import { shallow } from 'enzyme';

import Tab from './Tab';

// it('renders correctly Tab', () => {
//   const wrapper = shallow(<Button primary>Primary Button</Button>);

//   expect(wrapper).toMatchSnapshot();
// });
